.data
    mesaj1: .asciiz "bir sayi giriniz: "
    mesaj2: .asciiz "sonuc: "


.text


main:

li $v0,4
la $a0,mesaj1
syscall

li $v0,5
syscall
move $t0,$v0

slt $t1, $t0, $zero  
beq $t1, $zero, yazdir

sub $t0,$zero,$t0

yazdir:

li $v0,4
la $a0,mesaj2
syscall


li $v0, 1
move $a0, $t0
syscall

bitir:

li $v0, 10
syscall
